# 평가지표·Top-N·결과 해석 가이드

이 문서는 교수님 피드백 중 **성능평가 연결 부족, 지표 의미 설명 부족, Top-N 설명 부족, 결과 활용 설명 부족**에 해당하는 부분을 따로 정리한 문서다.

## 1. 왜 평가지표를 두 종류로 나눠 봐야 했는가

이 프로젝트는 성격이 두 가지를 동시에 가진다.

1. **분류 문제**
   - 각 카테고리가 상승할지, 상승하지 않을지를 구분해야 한다.
2. **추천·순위 문제**
   - 핵심 10개 분야 중에서 실제로 더 주목할 만한 분야를 Top-5로 뽑아야 한다.

그래서 평가도 하나의 지표만으로는 부족했다.

- `Accuracy`, `Balanced Accuracy`, `Precision`, `Recall`, `F1-score`, `ROC AUC`
  - 기본 분류 성능 확인
- `Precision@5`, `Recall@5`, `HitRate@5`, `NDCG@5`
  - Top-5 선별 성능 확인

즉 이 프로젝트는 **분류 문제이면서 동시에 순위 추천 문제**이기 때문에 두 종류의 지표를 함께 사용했다.

## 2. Top-N은 정확히 무엇을 뜻하는가

Top-N은 전체 후보 중 **예측 점수가 높은 상위 N개를 뽑는 방식**이다.

이 프로젝트에서는

- 전체 후보: 핵심 10개 유튜브 분야
- N: 5

로 두었기 때문에, 최종 문제는 **Top-5 상승 분야 선별 문제**가 된다.

즉 모델은 “어느 한 분야가 상승하냐 아니냐”만 맞히는 것이 아니라,  
**핵심 10개 중 어떤 5개를 가장 먼저 볼 것인가**를 정렬하는 역할도 함께 한다.

## 3. 분류 지표는 각각 무엇을 뜻하는가

### Accuracy

전체 예측 중 맞힌 비율이다.  
가장 직관적이지만, 클래스 비율이 치우칠 때는 단독으로 보기 어렵다.

### Balanced Accuracy

상승 클래스와 비상승 클래스를 **공평하게 나눠서 평균적으로 얼마나 잘 맞췄는지**를 보는 지표다.  
이 프로젝트처럼 상승 구간이 상대적으로 적을 수 있는 문제에서는 더 중요하다.

### Precision

모델이 “상승할 것”이라고 예측한 분야 중 실제로 상승한 비율이다.  
즉, **상승이라고 찍은 결과의 신뢰도**를 뜻한다.

### Recall

실제로 상승한 분야 중에서 모델이 상승이라고 찾아낸 비율이다.  
즉, **놓치지 않고 얼마나 잘 잡았는가**를 뜻한다.

### F1-score

Precision과 Recall을 함께 본 조화평균이다.  
한쪽만 높고 다른 쪽이 낮은 모델보다, **둘 사이의 균형**을 본다.

### ROC AUC

상승 분야와 비상승 분야를 **확률적으로 얼마나 잘 구분하는가**를 보는 지표다.  
값이 높을수록 상승/비상승을 더 잘 분리한다고 해석할 수 있다.

## 4. Top-5 지표는 각각 무엇을 뜻하는가

### Precision@5

모델이 Top-5로 추천한 분야 중 실제로 상승한 분야의 비율이다.  
즉, **Top-5 추천 결과의 정확도**다.

### Recall@5

실제로 상승한 분야 중에서 Top-5 안에 포함된 비율이다.  
즉, 중요한 상승 분야를 얼마나 상위권에 담았는지를 본다.

### HitRate@5

평가 구간마다 실제 상승 분야가 Top-5 안에 최소 하나 이상 포함되었는지를 본다.  
값이 1이면, **완전히 빗나간 적이 없었다**는 뜻이다.

### NDCG@5

정답을 Top-5 안에서 얼마나 높은 순위에 잘 배치했는지를 보는 지표다.  
Top-5 안에 들어오기만 하는 것이 아니라, **더 중요한 정답을 더 위에 두었는가**까지 반영한다.

## 5. 최종 성능은 어느 정도였는가

핵심 10개 분야 기준 최종 BiGRU 모델의 성능은 아래와 같았다.

### 분류 성능

- Accuracy: `0.767`
- Balanced Accuracy: `0.798`
- Precision: `0.944`
- Recall: `0.739`
- F1-score: `0.829`
- ROC AUC: `0.845`

### Top-5 선별 성능

- Precision@5: `0.900`
- Recall@5: `0.801`
- HitRate@5: `1.000`
- NDCG@5: `0.881`

## 6. 이 숫자를 어떻게 해석해야 하는가

이 성능을 과장 없이 해석하면 아래 정도가 가장 적절하다.

- 분류 성능도 전반적으로 안정적이었다.
- 특히 `Balanced Accuracy 0.798`, `ROC AUC 0.845`는 상승/비상승 구분 능력이 꽤 괜찮다는 뜻이다.
- 하지만 이 프로젝트의 진짜 목적은 분류 자체보다 **Top-5 상승 분야 선별**이다.
- 그 기준에서는 `Precision@5 0.900`, `NDCG@5 0.881`이 더 중요하다.

즉 이 모델은 “모든 분야를 완벽히 맞히는 범용 예측기”라기보다,  
**핵심 10개 분야 안에서 앞으로 더 주목할 분야를 상위권으로 뽑아주는 모델**로 이해하는 것이 맞다.

## 7. 최종 Top-5 결과는 무엇이었는가

최종 Top-5 상승 예측 분야는 아래와 같았다.

1. 반려동물
2. 먹방
3. 경제
4. 브이로그
5. 교육

이 결과는 단순 조회수 순위가 아니라,

- 상승 확률
- 순위 상승 확률
- 최근 12주 추세
- Google Trends 검색 관심도
- 상대 순위 신호

를 함께 반영한 결과다.

## 8. 예측 결과는 어디에 쓸 수 있는가

이 결과는 단순히 “재미있는 예측”에서 끝나지 않는다.

- **크리에이터**
  - 다음 달 어떤 분야 주제를 더 우선적으로 볼지 참고할 수 있다.
- **마케터**
  - 광고 집행이나 협업 대상을 정할 때 상승 가능성이 있는 분야를 먼저 검토할 수 있다.
- **플랫폼**
  - 앞으로 주목받을 분야를 미리 파악해 추천 전략이나 탐색 노출에 반영할 수 있다.

즉 결과의 의미는 **현재 인기 분야를 반복해서 보여주는 것**이 아니라,  
**상승 조짐이 있는 분야를 미리 포착하는 것**에 있다.

## 9. 발표에서 어떻게 이어서 말하면 좋은가

발표에서는 아래 정도로 연결하면 가장 자연스럽다.

> 본 프로젝트는 단순히 상승 여부만 맞히는 것이 아니라, 실제로 주목할 분야를 Top-5로 추천하는 것이 중요했습니다.  
> 그래서 Accuracy나 ROC AUC 같은 기본 분류 지표와 함께, Precision@5와 NDCG@5 같은 Top-5 선별 지표를 같이 사용했습니다.

## 한 문장 요약

이 프로젝트의 성능평가는 **“상승/비상승을 얼마나 잘 구분했는가”와 “실제로 주목할 분야를 Top-5 안에 얼마나 잘 올려놓았는가”를 함께 보는 구조**로 이해하는 것이 가장 정확하다.
