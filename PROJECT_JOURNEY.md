# 프로젝트 일대기

이 프로젝트는 처음부터 “BiGRU로 핵심 10개 분야를 예측하자”로 시작한 작업이 아니었다.  
출발점은 더 단순했다.

> 유튜브에서 앞으로 어떤 분야가 더 주목받을까?

처음에는 이 질문을 개별 영상 단위에서 풀 수 있을 거라고 생각했다.  
조회수, 좋아요, 댓글, 제목, 태그 같은 정보만 잘 정리하면 어느 정도 예측할 수 있을 것 같았다.

하지만 데이터를 정리할수록, 우리가 진짜 풀고 싶었던 문제는 “이미 잘된 영상을 설명하는 것”이 아니라 **앞으로 상승할 가능성이 있는 분야를 미리 선별하는 것**에 더 가까웠다.

이 문서는 그 질문이 어떻게 프로젝트 구조로 바뀌었는지, 어디서 막혔는지, 어떤 기준으로 최종 모델과 발표 방향을 정리했는지를 기록한다.

## 1. 출발점은 개별 영상 예측이었다

처음에는 자연스럽게 영상 단위 신호부터 봤다.

- 조회수
- 좋아요
- 댓글
- 제목
- 태그
- 업로드 시간

이 정도 정보면 “잘 될 영상”을 어느 정도는 가늠할 수 있을 것 같았다.  
실제로 많은 virality 예측 문제도 이 방향에서 출발한다.

그런데 작업을 계속하면서 한계가 분명해졌다.  
비슷한 초기 반응을 보인 두 영상도 **어떤 카테고리 흐름 위에 있는지**에 따라 의미가 달라졌기 때문이다.

그래서 질문을 바꾸게 됐다.

- “어떤 영상이 잘 될까?”보다
- **“어떤 분야가 앞으로 더 올라올까?”를 먼저 보자**

이게 프로젝트 전체를 바꾼 첫 번째 전환점이었다.

## 2. 카테고리 흐름을 먼저 보자는 방향으로 바뀌었다

이후 프로젝트의 중심은 개별 영상에서 카테고리 흐름 쪽으로 이동했다.

우리가 세운 핵심 가정은 아래와 같았다.

1. 분야마다 기본 체력이 다르다.
2. 영상 성과는 그 카테고리의 분위기와 완전히 분리되지 않는다.
3. 초기 반응은 중요하지만, 카테고리 흐름 위에서 읽을 때 더 의미가 커진다.

즉, 단순 virality 예측이 아니라  
**카테고리 트렌드 + 최근 시계열 반응 + 외부 변수**를 함께 묶는 쪽으로 프로젝트가 기울었다.

## 3. 데이터는 세 갈래로 정리됐다

최종 발표 기준으로 데이터는 아래 세 축으로 정리됐다.

### 과거 데이터

- 카테고리별 영상 성과
- 장기적인 조회수 흐름
- 분야의 기본 체력과 계절성

### 현재 데이터

- 영상 메타데이터
- 제목, 태그, 업로드 시간

### 시계열 및 외부 변수 데이터

- 최근 12주 카테고리 반응 추세
- Google Trends 검색량
- 공휴일, 연휴, 방학, 시험기간 등 캘린더 변수

중요했던 건 단순히 데이터를 많이 모으는 일이 아니었다.  
오히려 **서로 다른 시간축과 의미를 가진 데이터를 한 문제로 묶는 일**이 더 어려웠다.

## 4. 실제로는 데이터 정합성이 가장 힘들었다

이 프로젝트에서 시간을 가장 많이 쓴 부분은 모델보다 데이터였다.

### 4-1. Google Trends를 바로 쓰기 어려웠다

외부 관심도 신호를 강화하기 위해 Google Trends를 붙이려 했지만,  
수집 안정성과 파일 구조가 생각보다 불안정했다.

그래서 우리는:

- 어떤 파일이 실제 주간 데이터로 볼 수 있는지 다시 확인하고
- active category 기준으로 다시 정리하고
- 최종적으로 모델에 넣을 수 있는 형태로 복구하는 과정

을 따로 거쳤다.

### 4-2. 주차 기준이 서로 달랐다

더 큰 문제는 `year_week` 기준이 파일마다 조금씩 다르게 해석되고 있었다는 점이다.  
겉으로는 같은 주차처럼 보여도 실제 날짜로 복원하면 어긋나는 경우가 있었다.

이 문제는 category-week 시계열을 만들 때 치명적이었다.  
그래서 ISO week 기준으로 다시 맞추고, `week_date`를 명시적으로 만들어 주차 축을 재구성했다.

### 4-3. 모든 분야를 한 번에 다루기에는 분포가 너무 불균형했다

전체 분야를 한꺼번에 보면,

- 최근 활동이 거의 없는 분야
- 표본 수가 적은 분야
- 주차별 관측 길이가 크게 다른 분야

가 함께 섞여 모델과 분석을 불안정하게 만들었다.

이 과정에서 “범위를 줄이는 것 자체가 문제 해결”이라는 걸 확인했다.

## 5. 그래서 문제를 핵심 10개 분야 기준으로 다시 정의했다

처음 목표는 유튜브 전체 분야의 미래 관심도와 성장 가능성을 예측하는 것이었다.  
하지만 실제 데이터를 확인해보니, 전체 분야를 동일 조건으로 다루면 신뢰도가 크게 떨어질 수 있었다.

그래서 최종적으로는:

- 활동이 충분한 16개 분야를 먼저 학습 대상으로 정리하고
- 발표와 최종 평가는 핵심 10개 분야를 기준으로 고정했다.

이건 단순히 범위를 줄여 성능을 좋게 보이려는 선택이 아니라,  
**예측 가능한 범위 안에서 더 안정적인 딥러닝 문제로 재설계한 것**에 가깝다.

## 6. 모델 설명도 그 방향을 따라 정리됐다

최종 발표에서는 모델을 네 단계로 설명했다.

1. 데이터 입력
2. Category Trend Model
3. BiGRU 기반 시계열 모델
4. Final Prediction

여기서 중요한 점은 모델 이름 자체보다,  
**카테고리 흐름을 먼저 읽고, 최근 12주 시계열과 외부 변수를 그 위에 결합한다**는 구조였다.

즉, 발표에서의 모델 설명은 “어떤 딥러닝 블록을 썼는가”보다  
**무엇을 먼저 보고, 무엇을 나중에 결합하는가**를 보여주는 역할이 더 컸다.

## 7. BiGRU는 RNN 계열에서 선택했다

최종 모델은 RNN 계열의 표준 시계열 딥러닝 모델인 BiGRU를 기반으로 했다.

- RNN은 순차 데이터를 처리하는 기본 구조이고
- GRU는 장기 의존성 문제를 완화한 개선형 구조이며
- BiGRU는 그 GRU를 양방향으로 확장해 최근 12주 흐름의 앞뒤 문맥을 함께 학습할 수 있다

우리 문제는 짧은 시계열 안에서 최근 반응의 변화 방향과 흐름을 같이 읽는 것이 중요했기 때문에,  
BiGRU가 최종 모델로 가장 자연스러웠다.

## 8. 파생변수도 같은 논리에서 나왔다

최종 발표에서는 아래 변수들을 핵심으로 설명했다.

- avg_virality
- engagement_rate
- rolling_4week_mean
- momentum_ratio
- competition_score
- opportunity_score
- rise_label
- rank_up

이 변수들은 단순히 feature를 많이 만들기 위한 것이 아니라,

- 지금 반응 규모가 어느 정도인지
- 최근 4주 흐름이 유지되는지
- 상승 속도가 붙는지
- 다른 분야 대비 상대적으로 경쟁력이 있는지

를 수치로 표현하기 위한 장치였다.

즉, 파생변수 전략은 모델 장식이 아니라  
**“상승 분야”를 어떻게 정의하고 읽을 것인가**를 수치화한 흔적에 가깝다.

## 9. 최종 평가는 Top-5 선별 문제로 정리됐다

최종 발표 기준으로 모델은 핵심 10개 분야 중  
**다음 4주 동안 상승 가능성이 높은 Top-5 분야를 선별하는 문제**로 정리됐다.

최종 성능은 아래와 같았다.

- Accuracy: 0.767
- Balanced Accuracy: 0.798
- Precision: 0.944
- Recall: 0.739
- F1-score: 0.829
- ROC AUC: 0.845
- Precision@5: 0.900
- Recall@5: 0.801
- HitRate@5: 1.000
- NDCG@5: 0.881

최종 Top-5 상승 예측 분야는:

1. 반려동물
2. 먹방
3. 경제
4. 브이로그
5. 교육

이었다.

이 결과는 단순 조회수 크기가 아니라,  
상승 확률, 순위 상승 확률, 최근 반응 추세, 검색 관심도, 상대 순위 신호를 함께 반영한 결과다.

## 10. 이 프로젝트가 남기는 것

이 프로젝트의 핵심은 성능 숫자 하나에 있지 않다.

더 중요한 건 아래에 있다.

1. 개별 영상 중심 예측만으로는 설명되지 않는 문제를 발견했다
2. 카테고리 흐름과 최근 시계열 반응을 함께 보는 구조로 문제를 다시 정의했다
3. 데이터 정합성 문제를 해결하는 일이 모델만큼 중요하다는 걸 확인했다
4. 전체 범위를 고집하기보다, 핵심 10개 분야 안에서 더 안정적인 예측 문제를 만들었다

즉, 이 프로젝트는 “딥러닝 모델 하나를 적용해봤다”는 기록보다  
**예측 문제를 더 적절한 형태로 다시 세우고, 그에 맞는 데이터와 구조를 만들어본 과정**을 담고 있다.

## 한 문장 요약

이 프로젝트는 **개별 영상 virality 예측의 한계를 출발점으로 삼아, 카테고리 트렌드와 최근 12주 시계열, 외부 변수를 함께 활용해 유튜브 핵심 10개 분야의 향후 4주 상승 가능성을 예측하는 BiGRU 기반 구조로 발전해 간 과정**을 담은 프로젝트다.
