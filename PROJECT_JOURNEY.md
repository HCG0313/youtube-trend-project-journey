# PROJECT JOURNEY

## 이 프로젝트를 왜 시작했는가

유튜브 콘텐츠 시장은 빠르게 변하고, 어떤 분야가 다음에 뜰지를 미리 파악하는 것은 실무적으로도 의미가 있습니다.  
처음에는 단순히 “인기 분야를 예측할 수 있을까?”라는 질문으로 시작했지만, 진행하면서 이 문제를 더 명확하게 다시 정의해야 했습니다.

최종적으로 이 프로젝트는 다음 질문으로 정리되었습니다.

> 최근 12주 시계열과 외부 변수를 활용해, 유튜브 핵심 10개 분야 중 앞으로 4주 동안 상대적으로 더 상승할 분야를 Top-5 형태로 선별할 수 있는가?

## 타임라인으로 보는 진행 과정

### 1차 접근: 전체 20개 분야를 한 번에 예측

초기에는 더 넓은 범위의 카테고리와 여러 방향의 예측 문제를 함께 다뤘습니다.

- 전체 20개 분야를 한 번에 예측
- 절대값 예측과 상승/비상승 분류 혼용
- Google Trends를 실시간 외부 변수로 활용 시도
- 월간, 분기, 주간 단위 모델을 동시에 탐색

하지만 이 단계에서는 모델 성능보다 데이터 정합성 문제가 더 큰 병목이라는 점이 드러났습니다.

### 2차 전환점: Google Trends와 주차 축 문제 발견

실제로 가장 크게 부딪힌 문제는 다음과 같았습니다.

- `pytrends` 수집 실패와 `429 / TooManyRequests`
- 기존 weekly 검색량 파일의 해상도 불일치
- `%Y-%U`와 ISO week 기준 충돌

이 시점에서 “모델을 더 바꾸는 것”보다 **입력 데이터와 시간축을 먼저 고쳐야 한다**는 결론에 도달했습니다.

### 3차 전환점: 데이터 정합성 복구

이 단계에서 아래 작업을 진행했습니다.

- Apify 기반 Google Trends 재수집
- Google Trends 정규화 및 병합
- ISO week 기준 재집계
- category-week 시계열 정렬 재구성

즉, 모델 성능을 높이기 전에 **데이터를 먼저 믿을 수 있는 상태로 만드는 작업**을 했습니다.

### 4차 전환점: 문제 범위 재정의

전체 20개 카테고리를 그대로 예측 대상으로 두면

- sparse category
- 낮은 최근 활동성
- 카테고리별 시계열 길이 차이

때문에 성능이 쉽게 흔들렸습니다.

그래서 최근 활동이 충분한 16개를 학습 대상으로 두고,  
최종 발표와 성능 평가는 **핵심 10개 분야**에 집중하기로 했습니다.

이 선택은 성능을 인위적으로 부풀린 것이 아니라, **예측 가능한 문제로 재정의한 것**에 가깝습니다.

### 5차 최종 정리: BiGRU 기반 Top-5 상승 분야 선별 모델

최종적으로는 BiGRU를 기반으로 한 Top-N 상승 분야 예측 구조를 사용했습니다.

- 최근 12주 시계열 입력
- category embedding
- 상승 확률과 순위 상승 확률의 multi-head 출력
- 최종 Top-5 선별

즉, 단순 조회수 예측이 아니라 **어떤 분야를 상위에 둘 것인가**를 푸는 문제로 정리했습니다.

## 이 과정에서 배운 것

이 프로젝트를 통해 확인한 가장 중요한 점은 다음과 같습니다.

1. **딥러닝 모델 자체보다 데이터 정합성이 더 중요할 수 있다.**
2. **문제를 줄이고 다시 정의하는 것이 성능 개선의 핵심일 수 있다.**
3. **Top-N 목적이면 분류 지표만이 아니라 랭킹 품질도 함께 봐야 한다.**
4. **좋은 프로젝트는 결과 숫자뿐 아니라, 문제를 어떻게 수정해 나갔는지가 중요하다.**

## 그래서 이 프로젝트의 가치가 무엇인가

이 저장소의 가치는 단순히 “BiGRU를 썼다”에 있지 않습니다.

- 어떤 데이터가 실제로 믿을 만한지 다시 확인했고
- 모델보다 먼저 데이터 문제를 고쳤고
- 전체 문제를 줄여 현실적인 목표로 다시 정의했고
- 그 결과 발표 가능한 수준의 Top-5 선별 모델까지 완성했습니다

즉, 이 프로젝트는 **성능 숫자만 보여주는 저장소**가 아니라,  
**딥러닝 프로젝트가 실제로 어떻게 흔들리고, 어떻게 수정되며, 어떻게 마무리되는지**를 보여주는 기록이라고 보는 편이 맞습니다.
