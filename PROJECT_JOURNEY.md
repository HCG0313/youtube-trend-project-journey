# PROJECT JOURNEY

## 왜 이 프로젝트를 시작했는가

유튜브 콘텐츠 시장은 빠르게 변한다.  
이미 뜬 분야를 설명하는 것도 의미가 있지만, 실제로 더 궁금했던 건 **“다음에 올라갈 분야를 미리 잡아낼 수 있는가”**였다.

처음에는 단순히 인기 분야 예측 문제처럼 보였지만, 진행할수록 이 프로젝트는 **모델 선택보다 데이터 정리와 문제 재정의가 더 중요한 작업**이라는 걸 알게 됐다.

최종적으로 우리가 정리한 질문은 아래와 같았다.

> 최근 12주 시계열과 외부 변수를 사용해, 유튜브 핵심 10개 분야 중 앞으로 4주 동안 상대적으로 더 상승할 분야를 Top-5 형태로 선별할 수 있는가?

## 타임라인으로 보는 진행 과정

### 1차 접근: 전체 20개 분야를 한 번에 예측

초기에는 가능한 한 넓게 잡고 싶었다.

- 전체 20개 분야를 한 번에 예측
- 조회수/반응 지표 기반 상승·비상승 분류
- Google Trends를 외부 변수로 활용
- 6월, Q3, Q4 같은 시점 비교도 함께 시도

하지만 이 단계에서 성능보다 먼저 드러난 건 **데이터 정합성 문제**였다.

### 2차 전환: Google Trends와 주차 축 문제 발견

중간 점검 과정에서 핵심 문제가 두 가지 드러났다.

1. Google Trends 수집이 안정적이지 않았다.  
   `pytrends` 호출이 자주 막혔고, 기존 캐시 파일도 진짜 weekly 데이터라고 보기 어려운 경우가 있었다.

2. `year_week` 기준이 섞여 있었다.  
   일부는 `%Y-%U`, 일부는 ISO week를 써서 같은 문자열이 서로 다른 주차를 가리키는 문제가 생겼다.

이 시점부터는 성능을 더 올리는 실험보다, **입력 축부터 다시 맞추는 작업**이 우선이 됐다.

### 3차 전환: 데이터 정합성 복구

실제로 한 작업은 아래와 같았다.

- Apify 기반으로 Google Trends를 다시 수집
- 카테고리별 검색량 파일을 정규화해서 재병합
- ISO week 기준으로 `year_week`와 `week_date`를 다시 구성
- category-week 단위 시계열을 다시 집계

이 단계에서 얻은 가장 큰 교훈은 단순했다.

> 모델이 좋아도 입력 축이 틀어져 있으면 결과는 흔들린다.

### 4차 전환: 문제 범위 재정의

전체 20개 분야를 그대로 유지하면,

- sparse category가 많고
- 최근 활동량이 너무 적은 분야가 섞이고
- 분야별 시계열 길이와 안정성 차이가 커서

성능이 계속 흔들렸다.

그래서 최근 활동이 충분한 16개 분야를 학습 대상으로 남기고,  
최종 발표와 평가 대상은 **핵심 10개 분야**로 재구성했다.

이건 성능을 보기 좋게 만들기 위한 선택이라기보다, **예측 가능한 문제로 다시 정의한 것**에 가깝다.

### 5차 정리: BiGRU 기반 Top-5 선별 모델

최종적으로는 BiGRU를 기반으로 한 Top-N 상승 분야 예측 구조로 정리했다.

- 최근 12주 시계열 입력
- category embedding
- Google Trends + 캘린더 변수
- 상승 확률과 순위 상승 확률을 함께 내는 multi-head 구조

즉 단순 조회수 예측이 아니라, **어떤 분야를 상위권에 둘 것인가**를 맞히는 방향으로 모델 목적도 정리했다.

## 이 과정에서 배운 점

이번 프로젝트에서 가장 크게 남은 건 아래 세 가지다.

1. **모델보다 데이터 축이 먼저다.**  
   주차 기준이 어긋나 있거나 외부 변수 병합이 틀어지면, 그 위에 어떤 딥러닝 모델을 얹어도 흔들린다.

2. **성능이 안 나올 때는 문제를 다시 정의해야 한다.**  
   전체 20개 분야를 고집하는 것보다, 핵심 10개 분야로 범위를 줄인 것이 오히려 더 설득력 있는 결과를 만들었다.

3. **Top-N 목적이면 Top-N 지표를 봐야 한다.**  
   Accuracy만 보는 것보다 Precision@5, NDCG@5가 실제 프로젝트 목표와 더 맞았다.

## 이 저장소가 남기는 것

이 저장소의 핵심은 “BiGRU를 썼다”에 있지 않다.

- 어디서 데이터가 틀어졌는지 발견했고
- 어떤 실험을 버리고 어떤 문제 정의를 선택했는지 기록했고
- 최종적으로 발표 가능한 형태까지 어떻게 정리했는지를 남겨두었다는 점이 더 중요하다.

그래서 이 저장소는 결과물을 보여주는 곳이기도 하지만,  
동시에 **프로젝트가 실제로 어떻게 흔들리고, 다시 정리되고, 마무리되는지 남기는 기록**이기도 하다.
