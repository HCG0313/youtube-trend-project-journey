# 최종 모델

## 최종 모델 한 줄 설명

최종 모델은 **RNN 계열의 표준 시계열 딥러닝 모델인 BiGRU를 기반으로, 유튜브 핵심 10개 분야의 향후 4주 상승 가능성을 예측하는 모델**이다.

## 모델 구조 그림

![BiGRU 기반 Top-N 상승 분야 예측 구조](./docs/assets/core10_bigru_horizontal_diagram_simple.png)

입력으로는 최근 12주 시계열, Google Trends 검색량, 캘린더 변수, category embedding을 사용하고,  
출력으로는 상승 확률과 순위 상승 확률을 계산해 최종 Top-5 상승 분야를 선별한다.

## 왜 BiGRU를 썼는가

모델 선택 논리는 아래 흐름으로 설명할 수 있다.

1. **RNN**
   - 순차 데이터를 시간 순서대로 처리하는 기본 구조
2. **GRU**
   - 기본 RNN보다 장기 의존성 문제를 덜 겪도록 개선한 구조
3. **BiGRU**
   - GRU를 양방향으로 확장해 시계열의 앞뒤 문맥을 함께 반영하는 구조

이 프로젝트는 최근 12주 흐름 전체를 보고 판단해야 했기 때문에,  
단순 시점별 비교보다 **시계열 패턴 자체를 학습하는 구조**가 필요했다.  
그래서 최종 모델로 BiGRU를 선택했다.

## 모델 입력

최종 모델 입력은 세 축으로 구성된다.

### 1. 내부 시계열 입력

- 최근 12주 카테고리 반응 추세
- 평균 반응 규모
- 영상 수
- 참여도
- 순위
- 이동평균
- 변화율

### 2. 외부 변수 입력

- Google Trends 검색량
- 공휴일
- 연휴
- 방학
- 시험기간

### 3. 카테고리 정보

- category embedding

## 모델 출력

모델은 각 분야마다 아래 값을 낸다.

- 상승 확률 (`rise_probability`)
- 순위 상승 확률 (`rank_up_probability`)
- 보조 랭킹 점수 (`ranking_score`)

최종적으로는 이 값을 조합해 `final_score`를 만들고, 그 점수로 Top-5를 고른다.

## 최종 점수 구조

코어 10개 최종 모델에서 선택된 조합은 아래와 같다.

- `ranking_score = 0.0`
- `rise_probability = 0.4`
- `rank_up_probability = 0.6`

즉 실질적으로는 아래 식에 가깝다.

> `final_score = 0.4 × 상승 확률 + 0.6 × 순위 상승 확률`

이 말은, 단순히 “오를 것 같은가”보다 **상대적으로 위로 치고 올라갈 것 같은가**를 더 중요하게 반영했다는 뜻이다.

## 평가 방식

모델 평가는 두 층으로 나눠 봤다.

### 분류 성능

- Accuracy
- Balanced Accuracy
- Precision
- Recall
- F1-score
- ROC AUC

### Top-5 선별 성능

- Precision@5
- Recall@5
- HitRate@5
- NDCG@5

이 프로젝트의 목적은 단순히 상승/비상승을 맞히는 것보다, **어떤 분야를 상위권에 둘지 고르는 것**에 더 가깝다.  
그래서 Top-5 선별 지표를 따로 본다.

## 해석할 때 주의할 점

이 모델은 해석형 모델은 아니다.  
BiGRU 기반이라 내부 hidden representation은 여전히 블랙박스 성격이 강하다.

따라서 이 모델은

- 완전히 설명 가능한 모델

이라기보다,

- 시계열 패턴을 학습하고 상승 신호를 점수화해 Top-5를 고르는 모델

로 이해하는 편이 더 정확하다.

## 최종 해석

이 프로젝트의 최종 모델은 **핵심 10개 분야 안에서, 향후 4주 동안 상대적으로 더 올라갈 분야를 Top-5 형태로 선별하는 BiGRU 기반 예측 모델**이다.
