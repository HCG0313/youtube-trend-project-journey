# 데이터·변수·라벨 가이드

이 문서는 최종 발표에서 부족하다는 피드백을 받았던 **목표 정의, 범위 재정의, 변수 유형, 파생변수 계산 방식, 라벨 정의**를 한곳에 정리한 문서다.

## 1. 이 프로젝트가 정확히 예측하는 것

이 프로젝트의 최종 목표는 아래 한 문장으로 정리할 수 있다.

> **최근 12주간의 유튜브 반응 데이터, Google Trends 검색량, 캘린더 변수를 활용해 핵심 10개 유튜브 분야 중 다음 4주 동안 상승 가능성이 높은 Top-5 분야를 예측한다.**

즉 이 프로젝트는

- 개별 영상의 조회수를 직접 맞히는 프로젝트가 아니고
- 유튜브 전체 분야를 무조건 다 예측하는 프로젝트도 아니며
- **핵심 10개 분야 안에서 상대적으로 더 올라갈 분야를 뽑는 Top-5 선별 문제**

로 정의되어 있다.

## 2. 왜 16개 분야를 거쳐 핵심 10개 분야로 갔는가

최종 발표에서는 “왜 전체 분야를 그대로 쓰지 않았는가”를 분명히 설명해야 한다.

처음에는 더 넓은 범위의 분야를 함께 다루려 했지만, 실제 데이터에서는 분야별로 아래 차이가 컸다.

- 최근 주간 업로드 수
- 주차별 데이터 연속성
- 결측치 비율
- 반응량의 안정성
- 해석 가능한 흐름 유무

그래서 먼저 **활동량이 충분한 16개 active 분야**를 학습 대상으로 남겼고,  
최종 발표와 평가는 그중에서도 **주간 흐름이 비교적 안정적이고 설명 가능한 핵심 10개 분야**로 고정했다.

핵심 10개 분야는 다음과 같다.

- 게임
- 경제
- 교육
- 뉴스시사
- 먹방
- 반려동물
- 뷰티
- 브이로그
- 요리
- 운동

이 선택은 성능을 좋게 보이기 위한 축소라기보다,  
**예측 가능한 범위 안에서 더 안정적인 시계열 딥러닝 문제를 만들기 위한 재정의**에 가깝다.

## 3. 변수는 어떻게 나눠서 봐야 하는가

발표에서는 변수명을 나열하는 것보다, **변수 유형과 역할**을 함께 설명하는 편이 훨씬 낫다.

| 변수명 | 변수 유형 | 역할 | 설명 |
| --- | --- | --- | --- |
| `category` | 범주형 | 입력 변수 | 유튜브 분야 카테고리 |
| `avg_virality` | 숫자형 | 입력 변수 | 해당 주차 카테고리의 평균 반응 규모 |
| `video_count` | 숫자형 | 입력 변수 | 해당 주차 카테고리 영상 수 |
| `engagement_rate` | 숫자형 | 입력 변수 | 좋아요와 댓글을 반영한 참여율 |
| `category_rank` | 숫자형 | 입력 변수 | 같은 주차 안에서의 카테고리 순위 |
| `holiday_days_in_week` | 숫자형 | 입력 변수 | 해당 주차의 공휴일 일수 |
| `is_holiday_week` | 이진형 | 입력 변수 | 공휴일이 포함된 주차인지 여부 |
| `is_long_weekend_week` | 이진형 | 입력 변수 | 연휴 주차인지 여부 |
| `is_vacation_period` | 이진형 | 입력 변수 | 방학 기간인지 여부 |
| `is_exam_period` | 이진형 | 입력 변수 | 시험기간인지 여부 |
| `search_interest` | 숫자형 | 입력 변수 | Google Trends 검색 관심도 |
| `rise_label` | 이진 라벨 | 정답 라벨 | 다음 4주 동안 의미 있게 상승했는지 여부 |
| `rank_up` | 이진 라벨 | 보조 정답 라벨 | 다음 4주 평균 순위가 더 좋아졌는지 여부 |

## 4. 파생변수는 어떻게 만들었는가

파생변수는 한 번에 만들어진 것이 아니라,  
**주간 집계 → 최근 변화량 계산 → 최종 상대 특성 계산** 순서로 도출되었다.

### 4-1. 기본 반응/효율 파생변수

- `engagement_rate`
  - 계산: `(avg_likes + avg_comments) / avg_views`
  - 의미: 조회수 대비 반응 참여 정도

- `competition_score`
  - 계산: `video_count`
  - 의미: 같은 주차 안에서 해당 분야에 얼마나 많은 영상이 올라왔는지

- `opportunity_score`
  - 계산: `avg_virality / video_count`
  - 의미: 영상 수 대비 반응 효율, 즉 상승 여력

### 4-2. 최근 추세 파생변수

- `lag1_avg_virality`
  - 계산: 1주 전 평균 반응 수준

- `lag2_avg_virality`
  - 계산: 2주 전 평균 반응 수준

- `trend_delta_1`
  - 계산: `이번 주 avg_virality - 직전 주 avg_virality`
  - 의미: 가장 최근 1주 변화량

- `trend_delta_2`
  - 계산: `직전 주 avg_virality - 2주 전 avg_virality`
  - 의미: 그보다 앞선 변화량

- `trend_acceleration`
  - 계산: `trend_delta_1 - trend_delta_2`
  - 의미: 최근 상승 속도가 더 붙는지, 둔화되는지

- `momentum_ratio`
  - 계산: `trend_delta_1 / lag1_avg_virality`
  - 의미: 직전 수준 대비 최근 반응 증가 속도

### 4-3. 이동평균·변동성 파생변수

- `rolling_4week_mean`
  - 계산: 최근 4주 평균 반응 수준
  - 의미: 단기 흐름의 기준선

- `rolling_4week_std`
  - 계산: 최근 4주 반응 표준편차
  - 의미: 단기 변동성

- `rolling_2week_mean`
  - 계산: 최근 2주 평균 반응 수준
  - 의미: 더 짧은 흐름 확인

- `stability_score`
  - 계산: `rolling_4week_mean / rolling_4week_std`
  - 의미: 평균 대비 변동성이 얼마나 작은지

### 4-4. 순위 기반 파생변수

- `category_rank`
  - 계산: 같은 주차 안에서 `avg_virality` 기준 순위
  - 의미: 절대 수치가 아니라 상대 위치

- `rank_change`
  - 계산: `직전 주 순위 - 이번 주 순위`
  - 의미: 최근 순위가 올라왔는지

- `category_rank_pct`
  - 계산: 순위를 0~1 상대 백분위로 변환
  - 의미: 핵심 10개 안에서 상대적으로 어느 정도 상위권인지

### 4-5. 최종 BiGRU 입력용 상대 특성

- `rel_log_to_roll4`
  - 계산: `현재 log_avg_virality - log(최근 4주 평균)`
  - 의미: 최근 평소 수준 대비 지금 얼마나 높은지

- `z_to_roll4`
  - 계산: `(현재 avg_virality - 최근 4주 평균) / 최근 4주 표준편차`
  - 의미: 최근 4주 기준 표준화된 상승 정도

- `video_rel_to_roll4`
  - 계산: `현재 log_video_count - log(최근 4주 평균 video_count)`
  - 의미: 최근 평균 대비 영상 수가 얼마나 달라졌는지

### 4-6. 검색량·캘린더 파생변수

- `search_delta_1`
  - 계산: 전주 대비 Google Trends 검색량 변화
  - 의미: 외부 관심도 변화

- `search_roll4`
  - 계산: 최근 4주 평균 Google Trends 검색량
  - 의미: 검색 관심도의 단기 평균 흐름

- `holiday_days_in_week`, `is_holiday_week`, `is_long_weekend_week`, `is_vacation_period`, `is_exam_period`
  - 계산: 주차 안의 공휴일/연휴/방학/시험기간 여부
  - 의미: 유튜브 소비 패턴에 영향을 주는 외부 일정 변수

## 5. 라벨 변수는 어떻게 만들었는가

### `rise_label`

`rise_label`은 **메인 정답 라벨**이다.

- 먼저 `main_log_growth`를 계산한다.
  - `main_log_growth = log(다음 4주 평균 반응) - log(현재 4주 평균 반응)`
- 그다음 카테고리별 학습 구간 성장률 분포에서 `quantile` 기준 threshold를 구한다.
- 최종 코어 10개 모델에서는 `quantile = 0.60`이 선택되었다.
- `main_log_growth`가 이 threshold 이상이면 `rise_label = 1`, 아니면 `0`이다.

즉 `rise_label`은 “조금만 오르면 상승”이 아니라,  
**카테고리별로 의미 있는 상승 구간**만 양성으로 본 라벨이다.

### `rank_up`

`rank_up`은 **보조 정답 라벨**이다.

- 현재 주차의 카테고리 순위와
- 다음 4주 평균 순위를 비교해
- 미래 평균 순위가 더 좋아지면 `rank_up = 1`, 아니면 `0`으로 둔다.

즉 `rank_up`은 단순 반응량 증가가 아니라,  
**다른 분야 대비 상대 순위가 실제로 올라가는가**를 본다.

## 6. 이 문서를 발표에 어떻게 쓰면 좋은가

교수님 질문에 답할 때는 아래 정도로 짧게 말하면 충분하다.

> 이 프로젝트는 최근 12주 유튜브 반응 데이터와 Google Trends, 캘린더 변수를 이용해 핵심 10개 분야 중 다음 4주 동안 더 올라갈 Top-5 분야를 뽑는 문제로 정의했습니다.  
> 변수는 숫자형 입력, 범주형 입력, 외부 변수, 정답 라벨로 나누어 설명했고, 파생변수는 최근 변화량, 이동평균, 순위 변화, 검색량 흐름을 수치화하는 방향으로 만들었습니다.
