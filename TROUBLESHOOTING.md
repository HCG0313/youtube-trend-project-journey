# TROUBLESHOOTING

이 문서는 프로젝트를 진행하면서 실제로 부딪힌 문제와, 그 문제를 어떤 근거로 확인하고 어떻게 고쳤는지를 정리한 기록이다.

## 1. Google Trends 수집이 불안정했다

### 문제

- `pytrends` 기반 수집이 자주 실패했다.
- 대표적으로 `429`, `TooManyRequests` 오류가 반복됐다.
- 기존에 들고 있던 weekly 파일도 실제 주간 간격과 잘 맞지 않는 경우가 있었다.

### 어떻게 확인했는가

- 수집 로그에서 `429` 오류가 반복적으로 발생했다.
- 기존 weekly 파일의 날짜 간격을 확인해보니, 일부는 7일 간격이 아니라 28일 또는 35일 수준으로 어긋나 있었다.
- active category 기준으로 병합했을 때, 보조 변수 역할을 해야 할 검색량이 거의 붙지 않는 경우가 많았다.

### 해결

- Apify 기반 수집으로 우회했다.
- 카테고리별 검색량 파일을 다시 정규화하고 병합했다.
- 복구한 검색량 파일을 최종 모델 입력에 반영했다.

### 관련 파일

- `train_active_category_rank_bigru.py`
- `project_ready_data/external_features/google_trends_weekly_active_categories_normalized.csv`

## 2. 주차 기준(`year_week`)이 섞여 있었다

### 문제

일부 파이프라인은 `%Y-%U`, 일부는 ISO week를 사용하고 있었다.  
즉, 같은 `year_week` 문자열이라도 서로 다른 날짜 축을 가리킬 수 있는 상태였다.

### 어떻게 확인했는가

- category-week 테이블에 `week 00` 값이 존재했다.
- 같은 주차 문자열을 서로 다른 로직으로 복원했을 때 실제 날짜가 달라지는 경우가 있었다.
- 모델 학습용 시계열과 외부 변수 병합 구간이 깔끔하게 맞지 않는 현상이 나타났다.

### 해결

- `week_utils.py`를 기준으로 ISO week 변환을 통일했다.
- `week_date`를 명시적으로 생성하고, category-week 단위 집계를 다시 만들었다.
- 이후 모든 시계열 입력과 외부 변수 병합을 `week_date` 기준으로 맞췄다.

### 관련 파일

- `week_utils.py`
- `build_category_trend_dataset.py`
- `integrate_project_ready_data.py`

## 3. 전체 20개 분야를 그대로 쓰면 성능이 흔들렸다

### 문제

- sparse category가 많았고
- 최근 활동이 거의 없는 분야가 섞여 있었으며
- 분야별 시계열 길이와 안정성 차이가 컸다.

이 상태에서는 모델이 한 번은 맞고 한 번은 흔들리는 경우가 잦았다.

### 어떻게 확인했는가

- 카테고리별 최근 관측 주차 수를 비교했을 때 편차가 컸다.
- 전체 16개/20개 기준 모델보다 핵심 10개 기준 모델이 더 안정적인 성능을 보였다.
- 예측 결과도 핵심 10개로 줄였을 때 훨씬 일관되게 해석 가능했다.

### 해결

- 최근 활동이 충분한 16개 분야만 학습 대상으로 남겼다.
- 최종 발표와 성능 평가는 핵심 10개 분야 기준으로 정리했다.

### 관련 파일

- `train_active_category_rank_bigru.py`
- `project_ready_data/model_outputs/dl_core10_category_rank_bigru_summary.json`

## 4. 설명력을 과장하면 안 되는 모델이었다

### 문제

최종 모델은 BiGRU 기반이라 내부 hidden representation은 여전히 블랙박스에 가깝다.  
따라서 “완전히 설명 가능한 모델”처럼 말하면 과장에 가깝다.

### 어떻게 확인했는가

- 모델 코드에 attention, SHAP, saliency map 같은 명시적 설명 기법은 들어가 있지 않다.
- 우리가 직접 설명 가능한 부분은 출력 확률과 최종 점수 조합 수준이었다.

### 해결

- 상승 확률과 순위 상승 확률을 따로 출력하도록 설계했다.
- 발표와 문서에서는 “완전한 해석형 모델”이 아니라, **상승 신호를 분리해서 보여주는 모델**로 설명했다.

### 관련 파일

- `train_active_category_rank_bigru.py`
- `FINAL_MODEL.md`

## 5. 노트북과 경로 의존성이 컸다

### 문제

- 노트북을 어느 폴더에서 여느냐에 따라 파일 로딩이 실패할 수 있었다.
- 한글 경로와 산출물 폴더 구조 때문에 팀 공유 시 혼란이 생길 수 있었다.

### 어떻게 확인했는가

- 같은 노트북이 환경에 따라 열리기도 하고 깨지기도 했다.
- 발표 준비 과정에서 팀원 공유 패키지를 따로 정리해야 할 필요가 생겼다.

### 해결

- 메인 노트북 구조를 정리하고, 경로 점검 셀을 넣었다.
- `프로젝트-공유패키지` 폴더를 별도로 만들어 팀 공유용 구조를 정리했다.

### 관련 파일

- `프로젝트.ipynb`
- `youtube_trend_project_pipeline.executed.ipynb`
- `build_project_shared_package.py`

## 정리

이번 프로젝트에서 성능이 올라간 이유는 모델 하나를 바꿔서가 아니었다.  
오히려

- 수집 실패를 복구하고
- 주차 축을 다시 맞추고
- 문제 범위를 줄이고
- 모델 목적을 Top-N 선별에 맞춘 것

이 네 가지가 합쳐졌기 때문에 최종 결과가 가능했다.
