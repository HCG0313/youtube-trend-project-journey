# TROUBLESHOOTING

이 문서는 프로젝트 진행 중 실제로 발생했던 핵심 문제와, 이를 어떻게 확인하고 해결했는지를 정리한 기록입니다.

## 1. Google Trends 수집 실패

### 문제

- `pytrends`를 사용한 실시간 검색량 수집이 자주 실패했습니다.
- 대표적으로 `429`, `TooManyRequests` 오류가 발생했습니다.
- 기존 캐시 파일도 weekly라는 이름과 달리 실제 주간 해상도와 맞지 않았습니다.

### 어떻게 확인했는가

- 수집 로그에서 `429` 오류가 반복적으로 발생했습니다.
- 기존 weekly 파일을 열어보니 실제 날짜 간격이 7일이 아니라 28일/35일 수준으로 불규칙했습니다.
- active category 기준 병합 커버리지가 매우 낮게 나와 보조 변수로 쓰기 어려웠습니다.

### 해결

- Apify 기반 수집으로 우회했습니다.
- 카테고리별 재수집과 최근 구간 보강을 진행했습니다.
- 정규화된 검색량 파일을 다시 생성해 모델 입력에 반영했습니다.

## 2. 주차 축(`year_week`) 불일치

### 문제

- 일부 파이프라인은 `%Y-%U`
- 일부 모델/후처리는 ISO week

를 사용하고 있었습니다.

즉 동일한 `year_week` 문자열이 서로 다른 날짜 축으로 해석될 수 있었습니다.

### 어떻게 확인했는가

- category-week 테이블에 `week 00` 값이 존재했습니다.
- 같은 주차 문자열을 다른 로직이 다른 날짜로 복원하는 상황을 코드 레벨에서 확인했습니다.

### 해결

- `week_utils.py`를 기준으로 ISO week 변환을 통일했습니다.
- category-week 데이터를 다시 집계하고 `week_date`를 명시적으로 추가했습니다.

## 3. 전체 20개 분야에서 성능 불안정

### 문제

- 전체 카테고리를 그대로 예측 대상으로 두면
  - sparse category
  - 낮은 최근 활동성
  - 불균형한 시계열 길이

때문에 성능이 쉽게 흔들렸습니다.

### 어떻게 확인했는가

- 카테고리별 최근 관측 주차 수를 비교했을 때 편차가 컸습니다.
- 전체 16개/20개 기준 모델보다 핵심 10개 기준 모델이 훨씬 안정적인 성능을 보였습니다.

### 해결

- 최근 활동이 충분한 16개를 학습 대상으로 제한했습니다.
- 최종 발표와 성능 보고는 핵심 10개 분야로 재구성했습니다.

이 과정을 통해 모델의 목적과 실제 데이터 조건을 더 잘 맞출 수 있었습니다.

## 4. 설명력 오해

### 문제

최종 모델은 BiGRU 기반이기 때문에 내부적으로는 여전히 블랙박스 성격이 있습니다.  
따라서 “완전한 해석형 모델”처럼 발표하면 과장될 위험이 있었습니다.

### 어떻게 확인했는가

- 모델 코드에 attention, SHAP, saliency map 같은 명시적 설명 기법이 들어 있지 않았습니다.
- 결과 해석은 출력 확률 수준에서만 가능했습니다.

### 해결

- 상승 확률과 순위 상승 확률을 따로 출력하게 설계했습니다.
- 결과 해석은 feature importance가 아니라 **예측 신호 분해 수준의 설명**으로 제한했습니다.

## 5. 노트북 및 경로 안정성 문제

### 문제

- 노트북 실행 시 현재 작업 경로에 따라 파일 로딩이 깨질 수 있었습니다.
- 일부 한글 경로와 파일명 때문에 환경에 따라 탐색이 불안정했습니다.

### 어떻게 확인했는가

- 동일한 파일이 환경에 따라 읽히기도 하고 실패하기도 했습니다.
- 공유용 패키지를 만들 때 메인 노트북 탐색과 폴더 삭제에서 실제 경로 이슈가 재발했습니다.

### 해결

- 메인 노트북 구조를 정리하고, 공유용 패키지를 별도로 만들었습니다.
- 실제 사용 파일만 다시 모은 `프로젝트-공유패키지`를 생성했습니다.

## 핵심 정리

이 프로젝트에서 성능 향상은 단순히 모델을 바꿔서 나온 것이 아닙니다.

- 데이터 수집 복구
- 주차 기준 재정렬
- 문제 범위 축소
- 최종 모델 재설계

이 네 가지를 함께 수행했기 때문에 최종 결과가 가능했습니다.
