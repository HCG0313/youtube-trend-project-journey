# 발표 Q&A 가이드

이 문서는 [QnA_Report.pdf](./QnA_Report.pdf)를 바탕으로,  
실제 최종 발표 자료와 코드, 최종 성능 파일을 다시 대조해 정리한 **발표용 답변 가이드**다.

원본 표의 방향은 유지하되, 발표에서 바로 말해도 되는 표현만 남기고 애매하거나 과장될 수 있는 부분은 정리했다.

## 1. 왜 LSTM이 아니라 BiGRU를 사용했나요?

**짧은 답변**

RNN 계열 가운데 GRU는 LSTM보다 구조가 단순해 파라미터 수가 적고, 현재 데이터 규모에서 과적합 위험을 줄이기에 더 적합하다고 판단했다.  
여기에 양방향 구조를 붙인 BiGRU를 사용해 최근 12주 시계열의 전후 문맥과 반응 가속도를 함께 읽도록 설계했다.

**보강 설명**

- 이 프로젝트의 핵심 입력은 개별 영상이 아니라 `카테고리-주차 단위 시계열`이다.
- 따라서 자연어 모델처럼 아주 깊은 구조보다, 비교적 가벼우면서도 시계열 흐름을 안정적으로 읽을 수 있는 구조가 더 중요했다.
- 발표에서는 “RNN 계열 중 장기 의존성 학습에 유리한 GRU를 바탕으로, 최근 12주 시계열의 앞뒤 문맥을 함께 반영하기 위해 BiGRU를 채택했다” 정도로 답하면 충분하다.

## 2. `rise_label`은 무엇을 기준으로 상승이라고 정의했나요?

**짧은 답변**

현재 4주 평균 반응 수준과 다음 4주 평균 반응 수준의 차이를 로그 성장률로 계산한 뒤,  
카테고리별 학습 구간 성장률 분포에서 **상위 quantile** 이상인 경우를 `상승(1)`로 정의했다.

**보강 설명**

- 최종 코어 10개 모델에서 선택된 quantile은 `0.60`이다.
- 단순히 성장률이 0보다 큰 경우를 모두 상승으로 보지 않고, `카테고리별로 의미 있는 상승 구간`만 양성으로 잡았다.
- 코드상으로는 `train_active_category_rank_bigru.py`의 `build_main_targets()`에서 카테고리별 threshold를 계산한다.

## 3. 캘린더 변수와 Google Trends를 넣었을 때 실제로 성능이 좋아졌나요?

**짧은 답변**

완전히 모든 지표가 일관되게 올라간 것은 아니지만,  
`calendar_search` 설정이 `calendar_only`보다 validation 기준에서 더 안정적인 Top-5 선별 성능을 보여 최종 모델로 채택되었다.

**보강 설명**

- 최종 채택 설정은 `calendar_search`, `quantile=0.60`이다.
- 같은 quantile에서 `calendar_only` 대비 `calendar_search`는  
  - Test `Balanced Accuracy`: `0.677 → 0.798`
  - Test `ROC AUC`: `0.783 → 0.845`
  로 좋아졌다.
- Test `Precision@5`는 `0.95 → 0.90`으로 소폭 낮아졌지만,  
  `NDCG@5`는 `0.921 → 0.881` 수준으로 유지되었고, validation 기준 NDCG가 더 좋아 최종 선택에 반영되었다.

**발표용 한 문장**

“외부 변수를 넣었을 때 모든 지표가 일률적으로 개선된 것은 아니었지만, validation 기준으로 Top-5 선별 안정성이 더 좋아 최종 모델은 Google Trends와 캘린더 변수를 함께 사용한 설정으로 결정했습니다.”

## 4. 왜 16개 분야 중 최종 발표는 10개 분야만 사용했나요?

**짧은 답변**

16개 active 카테고리 중에서도 일부 분야는 주간 업로드 수와 반응량이 너무 적거나 결측과 변동성이 커서,  
최종 발표에서는 데이터 연속성과 해석 가능성이 높은 핵심 10개 분야만 고정해 평가했다.

**보강 설명**

- active 16개 분야는 다음과 같다.  
  `게임, 경제, 공부, 교육, 뉴스시사, 먹방, 반려동물, 뷰티, 브이로그, 여행, 요리, 운동, 음악, 인테리어라이프, 자동차, 테크`
- 이 중 발표 핵심 10개는  
  `게임, 경제, 교육, 뉴스시사, 먹방, 반려동물, 뷰티, 브이로그, 요리, 운동`
  이다.
- 제외된 6개는  
  `공부, 여행, 음악, 인테리어라이프, 자동차, 테크`
  로, 시계열 길이와 변동성 측면에서 상대적으로 불안정했다.

## 5. Google Trends와 YouTube API 데이터의 주차는 어떻게 맞췄나요?

**짧은 답변**

두 데이터 모두 `ISO week` 기준으로 다시 정리한 뒤, `year_week`와 `week_date` 축을 통일해 병합했다.

**보강 설명**

- 이 프로젝트에서 중요한 디버깅 포인트 중 하나가 `%Y-%U`와 ISO week의 불일치였다.
- 최종 전처리에서는 월요일 시작 주차 기준으로 일관된 `category-week` 축을 만들고,  
  그 위에 Google Trends와 캘린더 변수를 결합했다.
- 발표에서 이 질문이 나오면 “데이터 정합성 문제를 먼저 해결하고 나서 모델을 학습했다”는 점까지 같이 강조하면 좋다.

## 6. Multi-head 구조의 손실 함수는 어떻게 구성했나요?

**짧은 답변**

최종 모델은 메인 상승 타깃 하나만 보는 구조가 아니라,  
상승 여부, 순위 상승 여부, 4주 단계별 상승 여부, 그리고 pairwise ranking 신호를 함께 학습하는 구조다.

**보강 설명**

- 코드상 손실 조합은 다음과 같다.
  - `main loss`: `0.30`
  - `rank loss`: `0.15`
  - `step loss`: `0.15`
  - `pairwise ranking loss`: `0.40`
- 즉 “메인 타깃 0.7, 보조 타깃 0.3”처럼 단순 2분할 구조는 아니고,  
  최종적으로는 `ranking-aware multi-task loss`에 더 가깝다.
- 발표에서는 “메인 상승 타깃에만 의존하지 않고, 순위 변화와 주차별 상승 패턴까지 함께 학습하도록 손실을 분산했다”고 설명하면 자연스럽다.

## 7. Precision@5가 높으면, 항상 큰 카테고리만 추천하는 편향은 없나요?

**짧은 답변**

그 위험은 분명히 있기 때문에, 최종 모델은 절대 반응 규모만 보지 않고 상대 성장률, 순위 백분위, 모멘텀 변수를 함께 넣었다.

**보강 설명**

- 최종 점수는 단순 조회수 예측값이 아니라  
  `상승 확률 0.4 + 순위 상승 확률 0.6` 조합으로 계산된다.
- 실제 Top-5 예측 결과에 `반려동물`, `교육`처럼 절대 규모가 가장 큰 축은 아니지만 최근 회복/상승 신호가 잡힌 분야가 포함된 점이 이 구조를 보여준다.
- 따라서 이 모델은 “항상 큰 분야를 추천하는 모델”이 아니라,  
  `상대적으로 치고 올라오는 분야를 상위권에 배치하려는 모델`로 보는 게 더 정확하다.

## 8. 반려동물, 먹방, 경제, 브이로그, 교육이 Top-5로 나온 결과는 합리적인가요?

**짧은 답변**

네. 이 결과는 단순히 조회수 절대 크기를 기준으로 뽑은 것이 아니라,  
최근 반응 추세, 검색 관심도, 상대 순위 신호를 함께 본 결과라는 점에서 충분히 합리적이다.

**보강 설명**

- `반려동물`과 `교육`은 대형 카테고리만큼 절대 규모가 크지 않더라도,  
  최근 회복과 관심도 상승 신호가 함께 잡혀 최종 점수가 높아졌다.
- `먹방`, `브이로그`, `경제`는 최근 반응 유지력과 상대적 순위 흐름이 양호하게 나타났다.
- 발표에서는 “조짐을 포착하는 문제”라는 점을 함께 말하면 해석이 더 자연스럽다.

## 9. 데이터 규모가 16,641개 영상이면 BiGRU에 부족하지 않나요?

**짧은 답변**

영상 수만 보면 많지 않을 수 있지만, 최종 모델의 실제 입력 단위는 개별 영상이 아니라 `카테고리-주차 단위 시계열`이다.  
또한 드롭아웃, 경량화된 hidden structure, category embedding 크기 제어로 과적합을 줄였다.

**보강 설명**

- 발표 기준 데이터 규모는  
  - 전체 통합 영상 `18,958`
  - 중복 제거 영상 `16,641`
  - 주간 추세 데이터 `1,711`
  - 시계열 지원 데이터 `5,770`
  이다.
- 이 프로젝트는 “대규모 비디오 한 건 한 건을 분류하는 문제”라기보다,  
  주간 단위로 요약된 시계열 예측 문제로 재구성한 프로젝트다.

## 10. 앞으로 세부 콘텐츠 유형까지 확장하려면 무엇이 더 필요할까요?

**짧은 답변**

현재는 카테고리 수준 예측이므로, 세부 콘텐츠 유형으로 확장하려면 텍스트 정보와 토픽 추출 단계가 추가되어야 한다.

**보강 설명**

- 제목, 태그, 설명, 댓글 텍스트를 더 적극적으로 활용해야 한다.
- 방법으로는
  - 토픽 모델링
  - 키워드 기반 서브클러스터링
  - BERT 계열 임베딩을 활용한 세부 토픽 추출
  같은 접근이 가능하다.
- 즉 다음 단계는 “어떤 카테고리가 오를까?”에서  
  “그 카테고리 안에서 어떤 세부 포맷이 오를까?”로 내려가는 것이다.

## 발표 직전 한 줄 요약

질문이 들어오면 아래 세 문장을 중심으로 기억하면 된다.

1. **BiGRU를 쓴 이유**: RNN 계열 중 GRU가 현재 데이터 규모에 더 가볍고, 양방향 구조로 최근 12주 흐름의 전후 문맥까지 반영할 수 있어서.
2. **핵심 10개로 줄인 이유**: 전체 16개 active 분야 중 일부는 결측과 변동성이 커서, 데이터 연속성이 높은 핵심 분야로 범위를 재정의했기 때문에.
3. **이 모델의 의미**: 단순 인기 분야 반복이 아니라, 향후 4주 동안 상대적으로 더 상승할 가능성이 높은 분야를 Top-5로 선별하는 모델이라는 점.
