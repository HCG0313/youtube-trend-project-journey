# 결과 요약

## 최종 성능 요약

핵심 10개 분야 기준 최종 코어 10개 BiGRU 모델의 성능은 아래와 같다.

### 분류 성능

- Accuracy: `0.767`
- Balanced Accuracy: `0.798`
- Precision: `0.944`
- Recall: `0.739`
- F1-score: `0.829`
- ROC AUC: `0.845`

### Top-5 선별 성능

- Precision@5: `0.900`
- Recall@5: `0.801`
- HitRate@5: `1.000`
- NDCG@5: `0.881`

## 왜 핵심 10개 모델로 정리했는가

최종 결과를 이해하려면 “핵심 10개 모델이 이전 설정보다 얼마나 나아졌는가”를 같이 보는 편이 좋다.

| 설정 | Accuracy | Balanced Accuracy | F1-score | ROC AUC | Precision@5 | NDCG@5 |
|---|---:|---:|---:|---:|---:|---:|
| 16개 active category 모델 | 0.529 | 0.628 | 0.500 | 0.696 | 0.750 | 0.848 |
| **핵심 10개 최종 모델** | **0.767** | **0.798** | **0.829** | **0.845** | **0.900** | **0.881** |

이 비교는 성능이 단순히 모델 구조만 바꿔서 좋아진 것이 아니라,  
**문제를 핵심 10개 분야 Top-5 선별로 다시 정의했기 때문에 안정화된 결과**라는 점을 보여준다.

즉 이 수치는 전체 유튜브 분야를 그대로 일반화한 결과가 아니라,  
**핵심 10개 분야 안에서 상승 후보를 선별했을 때의 성능**으로 해석하는 것이 맞다.

한 문장으로 줄이면, 이 모델은 모든 분야를 넓게 맞히는 범용 예측기라기보다  
**핵심 분야 안에서 어디가 더 올라갈지를 고르는 선별형 모델**에 가깝다.

## 이 수치가 말해주는 것

- 분류 지표 기준으로도 상승/비상승을 구분하는 능력이 비교적 안정적이다.
- 특히 Top-5 선별 지표가 높다는 건, 실제로 주목해야 할 상승 분야를 상위권에 잘 배치했다는 뜻이다.

즉 이 모델은 모든 분야를 넓게 맞히는 범용 모델이라기보다,  
**핵심 10개 분야 안에서 어떤 분야가 더 올라갈지를 고르는 데 강한 모델**에 가깝다.

## 최종 Top-5 상승 예측 분야

최종 예측 결과는 아래와 같다.

1. 반려동물 `0.481`
2. 먹방 `0.467`
3. 경제 `0.460`
4. 브이로그 `0.458`
5. 교육 `0.429`

그다음 후보까지 포함하면 아래와 같다.

6. 요리 `0.408`  
7. 운동 `0.382`  
8. 게임 `0.375`  
9. 뉴스시사 `0.347`  
10. 뷰티 `0.337`

## 점수 해석

이 값은 단순 인기 점수나 조회수 예측값이 아니다.

- 각 분야의 상승 확률
- 각 분야의 순위 상승 확률

을 합쳐 만든 **최종 상승 점수(`final_score`)**다.

값이 높을수록, 핵심 10개 분야 안에서 앞으로 4주 동안 상대적으로 더 상승할 가능성이 높다고 해석할 수 있다.

## 발표에서 어떻게 말하면 좋은가

가장 무리 없는 표현은 아래와 같다.

> 반려동물, 먹방, 경제, 브이로그, 교육 분야가 향후 4주 동안 상대적으로 더 상승할 가능성이 높은 핵심 분야로 예측되었다.

## 남는 한계

- 전체 20개 분야가 아니라 핵심 10개 분야에 맞춘 결과다.
- BiGRU 내부를 세부적으로 설명하는 해석 기법까지 포함한 모델은 아니다.
- 검색량 변수는 복구해서 사용했지만, 실시간 live feed보다는 정리된 캐시 기반 입력에 가깝다.

## 한 줄 정리

최종 모델은 **핵심 10개 분야 안에서 향후 4주 동안 상대적으로 더 상승할 분야를 Top-5로 선별하는 데 충분히 방어 가능한 성능**을 보였다.
